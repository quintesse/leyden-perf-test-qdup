name: leyden-perf-test-qdup

scripts:
  test_setup:
  - sh: echo This is where we do the setup
  
  test_run:
  - sh: echo This is where we run the test

  test_cleanup:
  - sh: echo This is where we do the cleanup

hosts:
  server: tschotan@localhost
  local: LOCAL

roles:
  test:
    hosts:
    - local
    setup-scripts:
    - test_setup
    run-scripts:
    - test_run
    cleanup-scripts:
    - test_cleanup

states:
